%h1 Listing books
.search-box
  = render 'search_form'
%table.table.table-striped
  %thead
    %tr
      %th Title
      %th Author
      %th Subject
  %tbody
    - @books.each do |book|
      %tr
        %td= book.title
        %td= book.author
        %td= book.subject.join(', ') if book.subject
